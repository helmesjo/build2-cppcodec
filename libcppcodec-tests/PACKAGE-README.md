# libcppcodec-tests - Header-only base64/base32/hex codec C++ library (tests)

This is a `build2` package containing the test suite for the
[`cppcodec`](https://github.com/tplgy/cppcodec) library.


## Importable targets

This package exports no targets.


## Configuration variables

This package provides no configuration variables.
